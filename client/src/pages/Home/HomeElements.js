import styled from "styled-components";

export const HomeContainer = styled.div`
  justify-content: center;
`;

export const HomeListing = styled.div`
  background: lightgray;
  padding: 0.7rem calc((100vw - 1030px) / 2);
  z-index: 10;
`;
